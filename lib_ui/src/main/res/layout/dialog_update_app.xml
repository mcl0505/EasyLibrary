<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:minHeight="322dp"
            android:layout_marginHorizontal="50dp"
            android:background="@drawable/icon_update_app">

            <com.easy.lib_ui.view.RadiusTextView
                android:id="@+id/updateSure"
                android:layout_width="0dp"
                app:rv_radius="6dp"
                android:layout_height="34dp"
                android:gravity="center"
                android:textColor="@color/white"
                app:rv_backgroundColor="@color/app_color"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toBottomOf="@id/updateContent"
                android:layout_margin="16dp"
                android:textSize="17sp"
                android:text="立即升级"/>

            <TextView
                android:id="@+id/updateContent"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:minHeight="80dp"
                android:gravity="center"
                app:layout_constraintBottom_toTopOf="@id/updateSure"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                android:layout_marginHorizontal="16dp"
                android:text="发现新版本"
                android:textSize="16sp"
                android:layout_marginTop="170dp"
                android:layout_marginBottom="60dp"
                android:textColor="@color/app_color"/>

        </androidx.constraintlayout.widget.ConstraintLayout>

        <ImageView
            android:id="@+id/updateCancel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/icon_update_app_close"
            android:layout_gravity="center_horizontal"
            android:padding="32dp"/>

    </LinearLayout>
</layout>